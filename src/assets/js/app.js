$(document).on("ready", function () {
  $(".owl-carousel").owlCarousel({
    items: 1,
    loop: true,
    center: true,
    nav: true,
    autoplay: true,
    autoplayTimeout: 2500,
    autoplayHoverPause: true,
  });
});
